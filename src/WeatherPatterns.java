/**
 * The class WeatherPatterns finds the longest span of days in which
 * each day’s temperature is higher than on the previous day in that sequence.
 *
 * @author Zach Blick
 * @author YOUR NAME HERE
 */

public class WeatherPatterns {


    /**
     * Longest Warming Trend
     * @param temperatures
     * @return the longest run of days with increasing temperatures
     */
    public static int longestWarmingTrend(int[] temperatures) {
        // TODO: Write your code here!
        // n is the length of temperatuers
        // Set an array with entry i being the length of longest warming trend ending at node i
        //  and the array itself is an array of size n, filled with 1s?
        // And then created a nested for loop with i going from 1 to n and j going from 1 to i
        // And then compare temperatures at these locations and update the lengths
        // And then return the max value in the array because that would be the longest length
        return 0;
    }
}
